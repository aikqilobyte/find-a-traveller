[build]
  command = "npm run build"
  publish = ".next"

# Netlify's Next.js runtime: handles App Router, Server Components,
# Server Actions, middleware and the redirects in next.config.ts.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
